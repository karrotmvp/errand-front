import React from "react";
import styled from "@emotion/styled";

type ApplyFormProps = {};

export default function ApplyForm({}: ApplyFormProps) {
  return <ApplyFormWrapper></ApplyFormWrapper>;
}

const ApplyFormWrapper = styled.div``;
