import React from "react";
import styled from "@emotion/styled";

type AlarmProps = {};

export default function Alarm({}: AlarmProps) {
  return <AlarmWrapper></AlarmWrapper>;
}

const AlarmWrapper = styled.div``;
